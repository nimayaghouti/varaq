export type {
  Account,
  Book,
  Genre,
  Order,
  OrderItem,
  Review,
  User,
} from '@prisma/client';

export { OrderStatus, Role } from '@prisma/client';

import type {} from 'altcha/types/react';
